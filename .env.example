# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# --- Google OAuth ---
# credentials.json and token.json are generated by the Google OAuth flow.
# Keep them at the repo root; they are gitignored.

# --- API Keys ---
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# Add project-specific keys below...
